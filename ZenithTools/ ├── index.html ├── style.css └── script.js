<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ZenithTools - أدوات رقمية تُحدث الفارق</title>
    <link rel="stylesheet" href="style.css" />
    <!-- Font Awesome للأيقونات -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>
<body>

    <!-- ===== HEADER ===== -->
    <header>
        <div class="container">
            <div class="logo">
                <i class="fas fa-rocket"></i>
                <span>ZenithTools</span>
            </div>
            <nav>
                <a href="#tools">الأدوات</a>
                <a href="#features">الميزات</a>
                <a href="#pricing">الأسعار</a>
                <a href="#" class="btn-outline">تسجيل الدخول</a>
                <a href="#" class="btn-primary">ابدأ مجانًا</a>
            </nav>
            <button class="menu-toggle" id="menuToggle"><i class="fas fa-bars"></i></button>
        </div>
    </header>

    <!-- ===== HERO ===== -->
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <span class="badge">🔥 أكثر من 12,000 مستخدم</span>
                <h1>أدوات رقمية <span>تُحدث الفارق</span></h1>
                <p>مجموعة متكاملة من الأدوات الاحترافية لزيادة الإنتاجية وتحقيق أرباح حقيقية.</p>
                <div class="hero-buttons">
                    <a href="#" class="btn-primary btn-large">ابدأ مجانًا <i class="fas fa-arrow-left"></i></a>
                    <a href="#" class="btn-outline btn-large">شاهد العرض <i class="fas fa-play"></i></a>
                </div>
                <div class="stats">
                    <div><span>4.9</span> <i class="fas fa-star"></i> تقييم</div>
                    <div><span>98%</span> رضا</div>
                    <div><span>+12K</span> مستخدم</div>
                </div>
            </div>
            <div class="hero-image">
                <div class="floating-card card-1"><i class="fas fa-chart-line"></i> تحليلات</div>
                <div class="floating-card card-2"><i class="fas fa-robot"></i> ذكاء اصطناعي</div>
                <div class="floating-card card-3"><i class="fas fa-cloud"></i> سحابي</div>
            </div>
        </div>
    </section>

    <!-- ===== TOOLS ===== -->
    <section class="tools" id="tools">
        <div class="container">
            <h2>أدواتنا <span>المميزة</span></h2>
            <p class="subtitle">أدوات مجانية واحترافية لتنمية أعمالك</p>
            <div class="tools-grid">
                <div class="tool-card">
                    <i class="fas fa-pen-fancy"></i>
                    <h3>كاتب الذكاء الاصطناعي</h3>
                    <p>أنشئ محتوى احترافي بثوانٍ باستخدام أحدث تقنيات الذكاء الاصطناعي.</p>
                    <span class="tag">مجاني</span>
                </div>
                <div class="tool-card">
                    <i class="fas fa-chart-pie"></i>
                    <h3>محلل البيانات</h3>
                    <p>حلل بياناتك واستخرج رؤى قيمة لاتخاذ قرارات ذكية.</p>
                    <span class="tag">شائع</span>
                </div>
                <div class="tool-card">
                    <i class="fas fa-qrcode"></i>
                    <h3>مولد QR</h3>
                    <p>أنشئ رموز QR مخصصة لعلامتك التجارية بلمسة احترافية.</p>
                    <span class="tag">مجاني</span>
                </div>
                <div class="tool-card">
                    <i class="fas fa-file-pdf"></i>
                    <h3>تحرير PDF</h3>
                    <p>دمج، تقسيم، وتحويل ملفات PDF بسهولة وسرعة.</p>
                    <span class="tag">جديد</span>
                </div>
                <div class="tool-card">
                    <i class="fas fa-calculator"></i>
                    <h3>حاسبة الربح</h3>
                    <p>احسب أرباحك المتوقعة من مشاريعك المختلفة بدقة.</p>
                    <span class="tag">مجاني</span>
                </div>
                <div class="tool-card">
                    <i class="fas fa-clock"></i>
                    <h3>مدير المهام</h3>
                    <p>نظّم وقتك ومهامك لزيادة إنتاجيتك اليومية.</p>
                    <span class="tag">قريبًا</span>
                </div>
            </div>
        </div>
    </section>

    <!-- ===== FEATURES ===== -->
    <section class="features" id="features">
        <div class="container">
            <h2>لماذا <span>ZenithTools</span>؟</h2>
            <div class="features-grid">
                <div><i class="fas fa-gem"></i><h4>مجاني 100%</h4><p>جميع الأدوات مجانية بدون أي رسوم خفية</p></div>
                <div><i class="fas fa-bolt"></i><h4>سريع وسهل</h4><p>واجهة بسيطة تعمل بسرعة فائقة</p></div>
                <div><i class="fas fa-lock"></i><h4>آمن ومحمي</h4><p>بياناتك مشفرة وآمنة تمامًا</p></div>
                <div><i class="fas fa-headset"></i><h4>دعم 24/7</h4><p>فريق دعم جاهز لمساعدتك في أي وقت</p></div>
            </div>
        </div>
    </section>

    <!-- ===== PRICING ===== -->
    <section class="pricing" id="pricing">
        <div class="container">
            <h2>خطط <span>مستقبلية</span></h2>
            <p class="subtitle">ابدأ مجانًا، وترقّى عندما تحتاج للمزيد</p>
            <div class="pricing-grid">
                <div class="plan free">
                    <h3>مجاني</h3>
                    <div class="price">0 <span>$</span></div>
                    <ul>
                        <li><i class="fas fa-check"></i> 5 أدوات مجانية</li>
                        <li><i class="fas fa-check"></i> استخدام يومي</li>
                        <li><i class="fas fa-check"></i> دعم أساسي</li>
                    </ul>
                    <a href="#" class="btn-outline">ابدأ الآن</a>
                </div>
                <div class="plan pro">
                    <span class="badge">الأكثر مبيعًا</span>
                    <h3>احترافي</h3>
                    <div class="price">9.99 <span>$</span></div>
                    <ul>
                        <li><i class="fas fa-check"></i> جميع الأدوات</li>
                        <li><i class="fas fa-check"></i> استخدام غير محدود</li>
                        <li><i class="fas fa-check"></i> دعم VIP</li>
                        <li><i class="fas fa-check"></i> تحليلات متقدمة</li>
                    </ul>
                    <a href="#" class="btn-primary">اشترك الآن</a>
                </div>
                <div class="plan business">
                    <h3>مؤسسات</h3>
                    <div class="price">29.99 <span>$</span></div>
                    <ul>
                        <li><i class="fas fa-check"></i> كل ما في الاحترافي</li>
                        <li><i class="fas fa-check"></i> فريق عمل (5+)</li>
                        <li><i class="fas fa-check"></i> دعم مخصص</li>
                        <li><i class="fas fa-check"></i> تكامل مخصص</li>
                    </ul>
                    <a href="#" class="btn-outline">تواصل معنا</a>
                </div>
            </div>
        </div>
    </section>

    <!-- ===== FOOTER ===== -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div>
                    <div class="logo"><i class="fas fa-rocket"></i> ZenithTools</div>
                    <p>أدوات رقمية ذكية لتحسين إنتاجيتك.</p>
                    <div class="social">
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-linkedin"></i></a>
                        <a href="#"><i class="fab fa-github"></i></a>
                        <a href="#"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div>
                    <h4>الأدوات</h4>
                    <a href="#">كاتب ذكاء اصطناعي</a>
                    <a href="#">محلل بيانات</a>
                    <a href="#">مولد QR</a>
                </div>
                <div>
                    <h4>الشركة</h4>
                    <a href="#">من نحن</a>
                    <a href="#">المدونة</a>
                    <a href="#">وظائف</a>
                </div>
                <div>
                    <h4>الدعم</h4>
                    <a href="#">الأسئلة الشائعة</a>
                    <a href="#">تواصل معنا</a>
                    <a href="#">سياسة الخصوصية</a>
                </div>
            </div>
            <div class="footer-bottom">© 2026 ZenithTools. جميع الحقوق محفوظة.</div>
        </div>
    </footer>

    <script src="script.js"></script>
</body>
</html>* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', system-ui, sans-serif;
}

:root {
    --primary: #6C3BF5;
    --primary-dark: #5228C7;
    --bg: #0B0A1A;
    --card-bg: #14132B;
    --text: #F0EEFF;
    --text-muted: #9896B0;
    --border: #2A2850;
    --radius: 16px;
    --shadow: 0 20px 60px rgba(108, 59, 245, 0.15);
}

body {
    background: var(--bg);
    color: var(--text);
    line-height: 1.7;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
}

/* ===== HEADER ===== */
header {
    padding: 20px 0;
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    background: rgba(11, 10, 26, 0.85);
    backdrop-filter: blur(12px);
    z-index: 100;
}

header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 24px;
    font-weight: 800;
    display: flex;
    align-items: center;
    gap: 10px;
}
.logo i { color: var(--primary); }

nav {
    display: flex;
    align-items: center;
    gap: 24px;
}
nav a {
    color: var(--text-muted);
    text-decoration: none;
    font-weight: 500;
    transition: 0.3s;
}
nav a:hover { color: var(--text); }

.btn-primary, .btn-outline {
    padding: 10px 24px;
    border-radius: 50px;
    font-weight: 600;
    text-decoration: none;
    transition: 0.3s;
    display: inline-block;
}
.btn-primary {
    background: var(--primary);
    color: #fff;
    border: none;
}
.btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(108, 59, 245, 0.4);
}
.btn-outline {
    border: 1.5px solid var(--border);
    color: var(--text);
}
.btn-outline:hover {
    border-color: var(--primary);
    background: rgba(108, 59, 245, 0.08);
}

.menu-toggle {
    display: none;
    background: none;
    border: none;
    color: var(--text);
    font-size: 28px;
    cursor: pointer;
}

/* ===== HERO ===== */
.hero {
    padding: 80px 0 60px;
}
.hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
}

.badge {
    background: rgba(108, 59, 245, 0.15);
    color: var(--primary);
    padding: 6px 16px;
    border-radius: 50px;
    font-size: 14px;
    font-weight: 600;
    display: inline-block;
    margin-bottom: 16px;
}

.hero h1 {
    font-size: 52px;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 16px;
}
.hero h1 span {
    background: linear-gradient(135deg, var(--primary), #A78BFA);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.hero p {
    color: var(--text-muted);
    font-size: 18px;
    max-width: 480px;
    margin-bottom: 32px;
}

.hero-buttons {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-bottom: 40px;
}
.btn-large {
    padding: 14px 32px;
    font-size: 17px;
}

.stats {
    display: flex;
    gap: 40px;
}
.stats div {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 15px;
    color: var(--text-muted);
}
.stats span {
    color: var(--text);
    font-size: 22px;
    font-weight: 700;
}
.stats i { color: #FBBF24; }

/* ===== HERO IMAGE ===== */
.hero-image {
    position: relative;
    min-height: 320px;
}
.floating-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    padding: 20px 28px;
    border-radius: var(--radius);
    display: inline-flex;
    align-items: center;
    gap: 12px;
    font-weight: 600;
    position: absolute;
    animation: float 6s ease-in-out infinite;
    box-shadow: var(--shadow);
}
.floating-card i { color: var(--primary); font-size: 22px; }

.card-1 { top: 0; left: 0; animation-delay: 0s; }
.card-2 { top: 40%; right: 0; animation-delay: 2s; }
.card-3 { bottom: 0; left: 30%; animation-delay: 4s; }

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-16px); }
}

/* ===== TOOLS ===== */
.tools, .features, .pricing {
    padding: 80px 0;
}
.tools h2, .features h2, .pricing h2 {
    font-size: 40px;
    text-align: center;
    margin-bottom: 8px;
}
.tools h2 span, .features h2 span, .pricing h2 span {
    background: linear-gradient(135deg, var(--primary), #A78BFA);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}
.subtitle {
    text-align: center;
    color: var(--text-muted);
    margin-bottom: 48px;
}

.tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
}
.tool-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    padding: 32px 24px;
    border-radius: var(--radius);
    transition: 0.3s;
    position: relative;
}
.tool-card:hover {
    transform: translateY(-8px);
    border-color: var(--primary);
    box-shadow: var(--shadow);
}
.tool-card i {
    font-size: 36px;
    color: var(--primary);
    margin-bottom: 12px;
}
.tool-card h3 {
    font-size: 20px;
    margin-bottom: 8px;
}
.tool-card p {
    color: var(--text-muted);
    font-size: 15px;
}
.tag {
    position: absolute;
    top: 16px;
    right: 16px;
    background: rgba(108, 59, 245, 0.12);
    color: var(--primary);
    font-size: 12px;
    font-weight: 600;
    padding: 4px 14px;
    border-radius: 50px;
}

/* ===== FEATURES ===== */
.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 32px;
    text-align: center;
}
.features-grid i {
    font-size: 40px;
    color: var(--primary);
    margin-bottom: 12px;
}
.features-grid h4 {
    font-size: 20px;
    margin-bottom: 4px;
}
.features-grid p {
    color: var(--text-muted);
    font-size: 15px;
}

/* ===== PRICING ===== */
.pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
}
.plan {
    background: var(--card-bg);
    border: 1px solid var(--border);
    padding: 40px 28px;
    border-radius: var(--radius);
    text-align: center;
    transition: 0.3s;
    position: relative;
}
.plan:hover {
    transform: translateY(-6px);
    border-color: var(--primary);
}
.plan.pro {
    border-color: var(--primary);
    box-shadow: var(--shadow);
}
.plan .badge {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--primary);
    color: #fff;
}
.plan h3 {
    font-size: 24px;
    margin-bottom: 8px;
}
.price {
    font-size: 48px;
    font-weight: 800;
    margin: 16px 0;
}
.price span {
    font-size: 20px;
    font-weight: 400;
    color: var(--text-muted);
}
.plan ul {
    list-style: none;
    margin: 24px 0 32px;
    text-align: right;
}
.plan ul li {
    padding: 6px 0;
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--text-muted);
}
.plan ul li i {
    color: var(--primary);
}
.plan .btn-primary, .plan .btn-outline {
    width: 100%;
    text-align: center;
}

/* ===== FOOTER ===== */
footer {
    border-top: 1px solid var(--border);
    padding: 60px 0 30px;
}
.footer-content {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 40px;
    margin-bottom: 40px;
}
.footer-content .logo {
    font-size: 22px;
}
.footer-content p {
    color: var(--text-muted);
    margin: 8px 0 16px;
}
.social {
    display: flex;
    gap: 12px;
}
.social a {
    color: var(--text-muted);
    font-size: 20px;
    transition: 0.3s;
}
.social a:hover { color: var(--primary); }
.footer-content h4 {
    margin-bottom: 12px;
}
.footer-content a {
    display: block;
    color: var(--text-muted);
    text-decoration: none;
    padding: 4px 0;
    transition: 0.3s;
}
.footer-content a:hover { color: var(--text); }
.footer-bottom {
    text-align: center;
    color: var(--text-muted);
    font-size: 14px;
    border-top: 1px solid var(--border);
    padding-top: 24px;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 992px) {
    .hero .container {
        grid-template-columns: 1fr;
        text-align: center;
    }
    .hero p { margin: 0 auto 32px; }
    .hero-buttons { justify-content: center; }
    .stats { justify-content: center; }
    .hero-image { min-height: 200px; }
    .floating-card { position: relative; display: inline-flex; margin: 8px; }
    .card-1, .card-2, .card-3 { position: relative; top: auto; left: auto; right: auto; bottom: auto; }
    .footer-content { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 768px) {
    nav { display: none; }
    .menu-toggle { display: block; }
    nav.open {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 80px;
        left: 0;
        right: 0;
        background: var(--bg);
        padding: 24px;
        border-bottom: 1px solid var(--border);
        gap: 16px;
    }
    .hero h1 { font-size: 36px; }
    .tools h2, .features h2, .pricing h2 { font-size: 30px; }
    .pricing-grid { grid-template-columns: 1fr; }
    .footer-content { grid-template-columns: 1fr; }
                      }
